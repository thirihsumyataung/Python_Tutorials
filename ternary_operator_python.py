#Ternary operator is an operator that takes three arguments or operands.
#The argumenst and result can be of different types

age = int (input ("How old are you ? >> " ))

message = " eligible to drink " if age >= 18 else "Not eligible"

print(message)